services:
  rookie-ssti:
    build: .
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: "change-this-secret-in-production"
      FLAG: "flag{ssti_username_unlocked}"
    restart: unless-stopped
